GEMINI_API_KEY=your_gemini_api_key_here
REDIS_URL=redis://localhost:6379/0

# Docker: gid of the host's docker group (needed by the app container to
# reach /var/run/docker.sock). Find yours with: stat -c '%g' /var/run/docker.sock
DOCKER_GID=125
